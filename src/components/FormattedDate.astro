---
interface Props {
  date: Date;
  classes?: string;
}

const { date, classes } = Astro.props;
---

<time class={classes} datetime={date.toISOString()}>
  {
    date.toLocaleDateString('en-us', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })
  }
</time>
