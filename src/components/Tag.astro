---
import Link from './Link.astro';

const { href, classes } = Astro.props;
---

{
  href ? (
    <li class={classes}>
      <Link href={href}>
        <slot />
      </Link>
    </li>
  ) : (
    <li class="text-foreground-muted text-sm">
      #<slot />
    </li>
  )
}
